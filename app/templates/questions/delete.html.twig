{% include 'default/_form.html.twig' with {
    page_title: 'questions.delete.header' | trans({'%title%': question.title}),
    form: form,
    submit_label: 'action.save'|trans,
    back_to_list_path: url('question_index'),
} %}