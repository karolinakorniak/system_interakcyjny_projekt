{% extends 'base.html.twig' %}

{% block title %}
    {{ 'authors.title'|trans }}
{% endblock %}

{% block body %}
    <div>
        {% for author in authors %}
            <div>{{ author.name }}</div>
            <div>{{ author.description }}</div>
        {% endfor %}
    </div>
{% endblock %}