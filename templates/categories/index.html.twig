{% extends 'base.html.twig' %}

{% block title %}
    Categories
{% endblock %}

{% block body %}
    <p>
        Tutaj beda kategorie
    </p>

    <div>
        {% for category in categories %}
            <div>{{ category.name }}</div>
        {% endfor %}
    </div>
{% endblock %}